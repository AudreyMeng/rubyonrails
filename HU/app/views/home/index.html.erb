<!--
  插入图片用image_tag,参数可以设置宽和高
  gravatar is just a MD5 representation of the email which can be done in question.rb model under app/models.

  class="mr-3"设置margin,margin是元素左右边距
-->
<div class="container">
  <% @questions.each do |q| %>

    <div class="media">
      <a href="" class="mr-3">
        <image class="media-object" src="<%= q.gravatar %>>">
      </a>
      <div class="media-body">
        <h5 class="media-heading mt-0"><%= q.email %> asked:</h5>
        <%= q.body %>
        <div>
          <a href="/questions/12" class="btn btn-success btn-xs">View Answers</a>
        </div>
      </div>
    </div>

  <% end %>
</div>